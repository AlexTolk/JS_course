const num = 5

num >= 6 ? 'number is greater than 6' : 'number is less than 6'

num >= 6
  ? 'number is greater than 6'
  : 'number is less than 6'

  let value = 11
  console.log(value >= 0 ? value : -value)

  value = -5
  const res = value >= 0 ? value : -value
  console.log(res)
